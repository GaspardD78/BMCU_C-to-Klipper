# Fichier de configuration Klipper pour le BMCU-C (CH32V203)
# Généré manuellement en se basant sur le Kconfig du portage.

# Activer les options de bas niveau pour voir toutes les configurations
CONFIG_LOW_LEVEL_OPTIONS=y

# Sélectionner l'architecture du microcontrôleur
CONFIG_MACH_CH32V20X=y

# Modèle exact du MCU
CONFIG_MCU="ch32v203"

# Paramètres de la puce
CONFIG_CLOCK_FREQ=144000000
CONFIG_FLASH_SIZE=0x10000
CONFIG_RAM_SIZE=0x5000

# Interface de communication
CONFIG_USBSERIAL=y
CONFIG_CH32V20X_USB=y

# Activer les timers PWM (par défaut)
CONFIG_HAVE_PWM_TIM1=y
CONFIG_HAVE_PWM_TIM4=y

# Options standards de Klipper (à conserver)
CONFIG_SERIAL_PORT=1
CONFIG_SERIAL_BAUD=1250000
CONFIG_USB_VENDOR_ID=0x1d50
CONFIG_USB_DEVICE_ID=0x614e
CONFIG_USB_SERIAL_NUMBER="12345"
CONFIG_WANT_GPIO_BITBANGING=y
CONFIG_WANT_SOFTWARE_I2C=y
CONFIG_WANT_SOFTWARE_SPI=y
CONFIG_INLINE_STEPPER_HACK=y
